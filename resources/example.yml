# a sample
database:
  - name: infludb
    db:
      name:
      - retentionPolicy: one hour
        env: dev
        valueTag:
      - retentionPolicy: one day
        valueTag:
        env:
          goV: 1.8
          javaV: 1.8
    url:
  - name: opentsdb
    url:

log:
  level: debug
  maxSize: 328
  path:

server:
  port: 9527
  tomcat:
      access-log-enabled: true
      access-log-pattern: common
      accesslog.directory:
  session:
      cookie:
        name: john_snow
